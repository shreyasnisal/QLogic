import {StyleSheet, Dimensions} from 'react-native'

export default StyleSheet.create({
    gateBtn: {
        backgroundColor: '#000',
        alignItems: 'center',
        justifyContent: 'center',
    },
    gateName: {
        color: '#fff',
    }
})